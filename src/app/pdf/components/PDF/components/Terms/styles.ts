import { StyleSheet } from "@react-pdf/renderer";

export const styles = StyleSheet.create({
  container: {
    marginBottom: 20,
  },
  heading: {
    fontWeight: 500,
    fontSize: 14,
    marginBottom: 8,
  },
});
